---
"@gonk/tool-registry-mcp": minor
---

Add a development MCP switchboard that keeps one HTTP MCP registration stable while safely selecting among worktree environments. Add a mountable Web-standard MCP handler and trusted request-context hook so applications can expose their registry from an existing framework route instead of starting a second HTTP server.
